const GetPartnerList = () => {
  return (state) => {
    const routingState = state.get('partnerlist');
    const currentboundry=routingState.get('partnerlist');
    return currentproject;
  };
};

export {GetPartnerList};
